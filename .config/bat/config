##############
# BAT CONFIG #
##############

# bat is a cat substitute with a bunch of other features
# you can pass it flags to control the output
# these are the flags I've got on by default

# Run `bat --list-themes` for a list of all available themes
--theme="Catppuccin-frappe"
--number
--color=always
--decorations=always

# disable automatic paging (if I want a pages, just pipe through `less`)
--paging=never
